/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['utfs.io'], // Ensure this domain matches the domain you're fetching images from
    },
};

export default nextConfig;
